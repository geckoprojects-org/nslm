<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.example.spi</groupId>
    <artifactId>nslm</artifactId>
    <version>${revision}</version>
  </parent>
  <artifactId>org.example.spi.weaver.java21</artifactId>
  <name>ServiceLoader weaving mediator for Java 21 (framework extension)</name>
  <description>The sources and bnd instructions of org.example.spi.weaver, built for Java 21 without the
    call site technique (CallSiteWeaver needs the Class-File API of Java 24+); always uses the constant
    pool technique. No sources of its own.</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi.core</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- embedded as private package, see the bnd.bnd of org.example.spi.weaver -->
    <dependency>
      <groupId>org.example.spi</groupId>
      <artifactId>org.example.spi.core</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- the sources of org.example.spi.weaver without CallSiteWeaver -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>weaver-sources</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/generated-sources/weaver</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/../org.example.spi.weaver/src/main/java</directory>
                  <excludes>
                    <exclude>**/CallSiteWeaver.java</exclude>
                  </excludes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compileSourceRoots>
            <compileSourceRoot>${project.build.directory}/generated-sources/weaver</compileSourceRoot>
          </compileSourceRoots>
        </configuration>
      </plugin>
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
