<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.example.spi</groupId>
    <artifactId>nslm</artifactId>
    <version>${revision}</version>
  </parent>
  <artifactId>org.example.spi.weaver</artifactId>
  <name>ServiceLoader weaving mediator (framework extension)</name>
  <description>Framework extension bundle with a WeavingHook that redirects ServiceLoader.load to
    ServiceLoaders.load. Works with any launcher. Java 25; org.example.spi.weaver.java21 builds the
    same sources for Java 21, without the call site technique.</description>

  <dependencies>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi.core</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- embedded as private package, see bnd.bnd -->
    <dependency>
      <groupId>org.example.spi</groupId>
      <artifactId>org.example.spi.core</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
